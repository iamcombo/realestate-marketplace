import { Card, Col, Container, Grid } from "@mantine/core";

const Profile = () => (
  <Container size={1280} my={40}>
    <Grid columns={24}>
      <Col md={6}>
        <Card>

        </Card>
      </Col>
      <Col md={18}>

      </Col>
    </Grid>
  </Container>
);

export default Profile;